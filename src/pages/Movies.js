import { NavLink } from 'react-router-dom';

const id = '5';

const Movies = () => {
  return (
    <div>
      <input />
      <NavLink to={id}>ID</NavLink>
    </div>
  );
};

export default Movies;
